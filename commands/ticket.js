const { CommandBuilder } = require("../Commands.js");

/*module.exports = {
  command: new CommandBuilder()
    .setName("ticket")
    .setDescription("Open a support ticket to contact the support (_obviously_), or to request a bot invite.")
    // TODO
}*/
